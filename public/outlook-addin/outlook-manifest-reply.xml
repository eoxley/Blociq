async function generateBlocIQReply(event) {
  try {
    const email = Office.context.mailbox.userProfile.emailAddress;

    const res = await fetch("https://www.blociq.co.uk/api/ask-ai-outlook", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "X-User-Email": email
      },
      body: JSON.stringify({ question: "Generate reply for this email" })
    });
    const data = await res.json();

    if (data.ok) {
      Office.context.mailbox.item.body.setSelectedDataAsync(
        data.answer,
        { coercionType: Office.CoercionType.Html }
      );
    }
  } catch (err) {
    console.error("BlocIQ reply error", err);
  } finally {
    event.completed();
  }
}
